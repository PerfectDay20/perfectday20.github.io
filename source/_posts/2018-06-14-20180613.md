---
title: 20180613
date: 2018-06-14 21:06:19
tags:
---
1. 上古版 Hive 0.8.0 通过 `INSERT OVERWRITE DIRECTORY hdfs://a/b/c SELECT ...` 将结果写入 hdfs 时，目录 `hdfs://a/b` 必须存在，因为整个过程实际是现将查询结果放在配置的 scratch 目录中，当查询完毕后，再 mv 到最后的路径，其实就是 rename，所以父目录不存在会报错
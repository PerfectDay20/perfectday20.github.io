---
layout: single
title:  "20180520 Parquet"
date:   2018-05-20 22:40:05 +0800
tags: Java
---
## 收获
1. Dataset orderBy = sort, 都是全局排序
2. Dataset.write().sortBy 是分区内排序，但是现在还不支持写入Parquet
3. 所以要减少体积的话，使用`Dataset.orderBy(col1...).coalesce(n).write().parquet()`，或者`Dataset.coalesce(n).sortWithinPartition(col1...).write().parquet()`，但是后者使用了coalesce后，并行任务数减少为n，所以运行很慢，所以建议使用前者
4. [Parquet官方](http://parquet.apache.org/documentation/latest/)建议row group size和hdfs block size相等，这样可以保证高读取速度，并且重建数据不需要读取其它block，减少了网络传递。但是大的row group size会导致高内存消耗。所以一个hdfs block里包含整数个row group也是可取的。[http://ingest.tips/2015/01/31/parquet-row-group-size/](http://ingest.tips/2015/01/31/parquet-row-group-size/)
5. 基于初步试验，根据cardinality大的列排序存储后的paruet文件更小




---
layout: single
title:  "20180516"
date:   2018-05-16 22:40:05 +0800
tags: Spark
---

## 收获
1. 在cluster模式下的Spark任务中，假如任务处在stage之间而没有显示kill链接，可以使用`yarn application -kill <app-id>`来取消任务；但本机上的任务进程没有被kill掉，还需手动kill
2. cache虽然可以避免重复计算和重复读取，但是在缓存大量比如TB级别的数据时还是很慢的，这时候可能不使用cache而读取两遍数据更快，需要对比验证
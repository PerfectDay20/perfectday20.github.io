---
layout: single
title:  "20180522 Spark与多线程"
date:   2018-05-22 22:40:05 +0800
tags: Java
---
## 收获
1. 在spark driver中也可以使用thread pool来并行处理
   1. 因为spark是并行处理数据的，所以在使用spark的时候就很容易忘掉java本身的多线程；两者结合，在一些数据稍微倾斜的stage的最后一个task，和在保存文件需要长时间运行时，其它并行的task可以获得计算资源。在处理大量重复数据时很有效，比如连续处理100天的数据，则可以开一个fixedThreadPool来处理，比手动分隔日期启动多个spark任务要方便
   2. 缺点就是spark UI中任务很乱，不便于分析，所以适用于已经经过优化后的程序 
   3. 直接点击spark UI中的kill也不会kill掉整个任务，因为不断会有新线程生成
   4. [参考链接](http://blog.cloudera.com/blog/2015/05/working-with-apache-spark-or-how-i-learned-to-stop-worrying-and-love-the-shuffle/)

2. join->sortWithPartitions->coalesce(5).write() 导致join只发生在5个节点
3. join->repartition(200)->sortWithPartitions->coalesce(5).write() 导致join发生在默认设置的spark.sql.shuffle.partitions的997个节点，奇怪


---
layout: single
title:  "20180523 Spark 写文件冲突"
date:   2018-05-23 22:40:05 +0800
tags: Spark
---
## 收获
1. 两个spark任务向同一个目录中写文件，会造成冲突，导致任务失败；而采用之前多线程的方式，向一个父路径中利用partitionBy写文件，会导致文件结果缺失。这两个问题都是由于多个任务同时使用在父目录下的_temporary，造成冲突。
   1. 目前的解决办法是每个任务写在自己的分区中
   2. 但这样在最后要保存时，使用select和partitionBy的时候就不能加入第一个分区列名
   3. [参考链接](https://stackoverflow.com/questions/38964736/multiple-spark-jobs-appending-parquet-data-to-same-base-path-with-partitioning)
2. parquet格式增加值完全相同的列，对文件体积影响小于百分之一，可忽略不计；例如是否按多个字段分区对体积无影响
3. 不能overwrite写到source路径中，因为开始执行的时候，source就被清空导致无法读取数据；和hive不同